export interface loginFormType {

    email?: string,
    password?: string,

}

export interface registerFormType {
    name?: string,
    email?: string,
    password?: string,
    confirmPassword?: string
}

